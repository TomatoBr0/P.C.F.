javascript:(function(){let iframe=document.querySelector('iframe#interactiveIframe');let w=iframe?iframe.contentWindow:window;let progress=w.progressLevelList;if(!progress){alert('No game state! Go to a Concept Builder first.');return;}if(!w.CAList&&!w.SRList&&!w.QAList){alert('ℹ️ Click CHECK ANSWER first.\n\nThen click this bookmarklet AGAIN on Continue screen.');return;}progress.fill(2);alert('🎮 ALL TASKS COMPLETED!\n\nNow click continue and then refresh the page.');})();
